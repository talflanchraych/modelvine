<% provide(:title, 'All users') %>
<h1>Admin Users Page</h1>

<%#= render "shared/search_form" %>

<%= will_paginate @users %>

<ul class="users span12">
  <% @users.each do |user| %>
      <%= render user %>
  <% end %>
</ul>

<%= will_paginate @users, class: 'pagination span8' %>

