<% provide(:title, 'All users') %>
<h1>All Users</h1>
<p>This Page only Lists users who have update their name in the Update Page</p>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
      <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
